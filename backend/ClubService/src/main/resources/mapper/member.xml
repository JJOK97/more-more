<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.member.model.mapper.MemberMapper">
	<resultMap type="memberDto" id="memberMap">
		<result column="user_id" property="userId"/>
		<result column="user_pwd" property="userPwd"/>
		<result column="user_email" property="userEmail"/>
	</resultMap>

	<insert id="register" parameterType="memberDto">
		insert into members (user_id, user_pwd, user_email) 
		values (#{userId}, #{userPwd}, #{userEmail})
	</insert>

	<select id="login" parameterType="memberDto" resultMap="memberMap">
		select * from members
		where user_id=#{userId} and user_pwd=#{userPwd}
	</select>

	<update id="saveToken" parameterType="String">
		 update members
		 set token = #{token}
		 where user_id = #{id};
	</update>
	<update id="removeToken" parameterType="String">
		update members
		set token = null
		where user_id = #{id};
	</update>

	<select id="getRefreshToken" parameterType="String" resultType="String">
		select token from members where user_id = #{id};
	</select>

	<select id="checkDuplicate" parameterType="String" resultType="String">
		select user_id from members where user_id = #{id};
	</select>

</mapper>